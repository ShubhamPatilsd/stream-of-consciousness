# Generate a long random value, for example: openssl rand -hex 32
PUBLISH_TOKEN=

# Fine-grained GitHub token: this repository only, Contents permission set to write
GITHUB_TOKEN=

# Optional. Voice thoughts publish raw Apple Dictation if this is not configured.
OPENAI_API_KEY=
OPENAI_CLEANUP_MODEL=gpt-5.4-nano
